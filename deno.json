{
  "tasks": {
    "generate": "deno task g:server && deno task g:client",
    "g:server": "deno run --allow-net --allow-read --allow-write generator/generate.ts server",
    "g:client": "deno run --allow-net --allow-read --allow-write generator/generate.ts client"
  }
}
